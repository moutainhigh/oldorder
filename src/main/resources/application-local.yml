spring:
  #数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/ek_old?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    connectionProperties: druid.stat.mergeSql=true

server:
  port: 8001

logging:
  level:
    cn.vpclub.moses: debug